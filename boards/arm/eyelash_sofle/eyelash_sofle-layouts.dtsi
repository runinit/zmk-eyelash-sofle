// Physical layout definitions for darknao ZMK fork compatibility
// Removed physical_layouts.dtsi dependency as it's not available in rgb-layer branch

/ {
    // Physical layout removed - not supported in rgb-layer-25.08 branch
    // This can be re-added when the branch includes physical layout support
};